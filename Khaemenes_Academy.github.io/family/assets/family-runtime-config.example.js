/*
  FUTURE SERVER CONFIGURATION EXAMPLE

  Do not put API keys, passwords, bearer tokens, SMTP credentials,
  signing secrets, or invitation secrets into this file.

  When the server exists, configure the shipped invite client at build/deploy time:

  enabled: true
  baseUrl: "https://accounts.vervenveda.com"

  Browser requests must use:
    credentials: "include"

  Authentication should be server-managed secure cookies.
*/
