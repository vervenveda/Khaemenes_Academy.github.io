// Khaemenes Academy protected Account Service runtime configuration.
// Public and non-secret. Authentication remains disabled until the protected
// service is commissioned and the release gate passes.
window.KHAEMENES_ACCOUNT_RUNTIME = Object.freeze({
  enabled: false,
  baseUrl: null,
  timeoutMs: 8000
});
